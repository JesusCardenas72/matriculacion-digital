import { MatriculacionSection, useMatriculacion } from './matriculacion';

function MyForm() {
  const { formData, handleChange, setConvalidacionAsignaturas } = useMatriculacion();

  return (
    <form>
      <MatriculacionSection
        formData={formData}
        onChange={handleChange}
        onConvalidacionClick={() => { /* abre tu modal */ }}
      />
    </form>
  );
}
